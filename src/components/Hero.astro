<section
  class="relative flex flex-col md:flex-row items-center justify-between min-h-screen px-6 py-16 md:py-24 overflow-hidden bg-cover bg-center"
  style="background-image: url('/images/nasa-hero.jpg');"
>

<!-- Background video -->
  <video
    autoplay
    muted
    loop
    playsinline
    poster="/images/nasa-hero.jpg"
    class="absolute inset-0 w-full h-full object-cover z-0"
  >
    <source src="/hero-bg-vid1_landscape.mp4" type="video/mp4" />
    <!-- Fallback text if video fails completely -->
    Your browser does not support the video tag.
  </video>

  <!-- Overlay removed, find in textfile -->
    <!-- Optional overlay for readability -->
  <div class="absolute inset-0 bg-black/30 z-0"></div>
  
  <!-- Containerize the left and right elements -->
  <div class="container mx-auto flex flex-col lg:flex-row items-center justify-evenly gap-4">

  <!-- LEFT: heading + description -->
  <div class="relative max-w-xl text-center bg-white/70 dark:bg-slate-900/50 backdrop-blur-md rounded-lg p-6 md:p-10 shadow-md z-10">
    <p class="text-sm leading-normal font-medium uppercase tracking-wider text-brand mb-3">
      Automate your Business Intelligence
    </p>
    <h1 class="text-4xl md:text-5xl font-extrabold text-slate-900 dark:text-white leading-normal mb-4">
      Implement Data Driven Decisions
    </h1>
    <p class="text-lg text-slate-700 dark:text-slate-300 mb-6">
      Optimize your spreadsheets and obtain data-driven insights your business needs to grow and expand!
    </p>
  </div>

  <!-- RIGHT: CTA buttons -->
  <div class="mt-10 md:mt-0 flex flex-col min-w-10 sm:flex-row gap-4 md:justify-end z-10">
    <a href="#signup" class="group inline-flex items-center justify-center px-6 py-3 rounded-md bg-brand text-white font-semibold shadow transition-all hover:bg-green-600">
      Start Now
      <span class="ml-2 transform transition-transform group-hover:translate-x-1">➔</span>
    </a>
    <a href="#features" class="group inline-flex items-center justify-center px-6 py-3 rounded-md border border-slate-200 text-slate-700 dark:text-slate-200 transition-all hover:border-green-500 hover:text-green-600 dark:hover:text-green-400">
      Learn More
      <span class="ml-2 transform transition-transform group-hover:translate-x-1">➔</span>
    </a>
  </div>
  
  </div>

<div id="features"></div>
</section>
